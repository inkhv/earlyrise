### earlyrise production env (copy to deploy/env.production)
### Do NOT commit deploy/env.production

# --- API ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PORT=3001

# --- BOT ---
TELEGRAM_BOT_TOKEN=
API_BASE_URL=http://127.0.0.1:3001

# --- Optional (later) ---
NHN_APP_KEY=
NHN_SECRET_KEY=

# --- Integrations (optional) ---
# If set, API will POST voice check-ins to n8n webhook and expect JSON response:
# { reply: string, transcript?: string, confidence?: number, raw?: any }
N8N_WEBHOOK_URL=

# --- OpenAI (recommended for MVP voice curator, without n8n) ---
OPENAI_API_KEY=
OPENAI_STT_MODEL=whisper-1

# Alternative (recommended): keep key in a file on the server (chmod 600)
OPENAI_API_KEY_FILE=/opt/earlyrise/deploy/openai_key.txt

# If set, API will use Prompt Builder via POST /v1/responses:
# - OPENAI_PROMPT_ID is like: pmpt_...
# - OPENAI_PROMPT_VERSION: usually "1"
OPENAI_PROMPT_ID=pmpt_694e9be46a1c8193bfcdf41a5eb0577e0cbea41717e119a3
OPENAI_PROMPT_VERSION=1

# Fallback when prompt ID is not set: use chat.completions with this model
OPENAI_CHAT_MODEL=gpt-4o-mini


