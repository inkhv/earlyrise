# EarlyRise — сценарии теста бота до 08-01 (MVP)

Цель: прогнать понятные сценарии так, чтобы мы получили данные для улучшений (бот/админка/оплата).

## 1) Онбординг (личка)
- `/start`
- `/settz GMT+3` (или `/settz` → отправить геопозицию 📍)
- `/join 07:00`
- `/me` (проверить, что timezone в GMT‑формате)

Ожидаемо:
- таймзона сохранена как `GMT±HH:MM`
- участие в активном челлендже создано

## 2) Отметка в общем чате (+)
- В общем чате отправить `+` (или `+07:00`, `+ текст`)

Ожидаемо:
- сообщение в чате не “засоряется” ответами бота (silent)
- в БД появляется `checkins` с `kind=group_plus`
- в админке на сводке за день появляется `+`

## 3) Voice check-in (личка)
- Отправить голосовое (отчёт о подъёме/планы на утро)

Ожидаемо:
- создаётся `checkins(source=voice)`
- появляется расшифровка/ответ куратора (через n8n)

## 4) Ограничение: 1 voice в сутки
- Отправить второе голосовое в тот же день

Ожидаемо:
- бот отвечает: “Голосовое уже принято сегодня… следующее завтра… вопросы — в общий чат”
- второй voice‑чек‑ин НЕ создаётся

## 5) Напарники (пока вручную через админку)
- В админке открыть карточку участника → назначить напарника (Assign)
- Проверить отображение “Напарник” в списке участников и в карточке
- Разорвать пару (Unpair)

Ожидаемо:
- создаётся/закрывается `buddy_pairs(status=active/inactive)`
- “Напарник” корректно отражается в UI

## 6) Что фиксировать (для обратной связи)
- где запутались (команды/сообщения/ошибки)
- качество ответов куратора (тон, длина, полезность микро‑шага)
- “окно чек‑ина” и таймзона: понятность/ошибки
- удобство отметок `+` в чате


