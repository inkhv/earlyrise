## HANDOFF — 2026-01-11

### Контекст / цель
Добавили сценарий “оплата → срок доступа → напоминания/кик из чата → восстановление участия”, без подписок/автосписаний.

### Текущий статус (prod)
- **Git SHA (main)**: `b8f7552`
- **VPS**: код задеплоен, сервисы `earlyrise-api` и `earlyrise-bot` перезапущены и в состоянии `active`.

### Что реализовано
- **Новые тарифы** (бот + API):
  - 30 дней — 490 ₽ (`plan_code=d30`)
  - 60 дней — 890 ₽ (`plan_code=d60`)
  - 90 дней — 1400 ₽ (`plan_code=d90`)
  - навсегда (поддержать проект) — 3000 ₽ (`plan_code=life`)
- **Окончание доступа**:
  - API считает `paid_until` (best-effort) по истории `payments` (по `plan_code`, а при отсутствии — по сумме для совместимости).
  - В `/bot/me` добавлен статус `expired` (если были paid-платежи, но срок уже истёк).
  - Бот в статусе `expired`:
    - блокирует чек‑ины (DM text/voice)
    - показывает только кнопку **«Восстановить участие»** → меню оплаты.
- **Ссылка на чат после оплаты (однократно)**:
  - После первого “paid” пользователю приходит сообщение с инвайтом в общий чат:
    - `https://t.me/+_9uHztv4J7MxMWNi`
  - Идемпотентность через `wallet_ledger.reason = chat_invite_sent:<challenge_id>`.
- **Cron-логика (ручной запуск через admin endpoint)**:
  - Добавлен `POST /admin/subscriptions/run`
  - Что делает:
    - напоминание **за 2 дня** до конца периода (DM)
    - сообщение **в день окончания** (DM) + выставляет `participations.left_at` (чтобы считался “не участник”)
    - **на следующий день** удаляет из чата (ban 60 сек + unban) — чтобы можно было вступить снова после оплаты
  - Идемпотентность:
    - если в БД нет новых колонок, маркеры пишутся в `wallet_ledger` (`sub:*`)
    - если колонки есть — используются `users.*_sent_at` / `users.kicked_from_chat_at` (best-effort).

### Важные env / конфиг
- **`MAIN_CHAT_ID`**: используется как id общего чата (подтверждено пользователем что “ок”).
- **`ADMIN_DASHBOARD_TOKEN`**:
  - выставлен на VPS в `/opt/earlyrise/deploy/env.production`
  - используется для авторизации всех `/admin/*` через header `x-admin-token`.
  - значение токена **не хранить** в репозитории.
- **`EARLYRISE_PUBLIC_CHAT_INVITE_URL`**:
  - выставлен на VPS в `/opt/earlyrise/deploy/env.production`
  - fallback в коде: `https://t.me/+_9uHztv4J7MxMWNi`

### Как проверить быстро
- **Меню оплаты**:
  - в боте: `/menu` → “💳 Оплатить участие” → 4 тарифа.
- **Ссылка на чат**:
  - после первой успешной оплаты: открыть `/menu` → должно прийти сообщение со ссылкой на чат (один раз).
- **Cron dry-run**:
  - `POST /admin/subscriptions/run` с `{"dry_run": true}` + header `x-admin-token: <ADMIN_DASHBOARD_TOKEN>`
  - вернёт счётчики кандидатов и ошибок, ничего не отправляя/не кикая.

### Файлы, которые менялись сегодня (ключевые)
- `apps/bot/src/handlers/menu.ts` — тарифы + UI для `expired` (“Восстановить участие”)
- `apps/bot/src/handlers/dm.ts` — блок чек‑инов для `expired`
- `apps/api/src/routes/checkin.ts` — тарифы + `expired` + chat invite offer
- `apps/api/src/routes/admin.ts` — `POST /admin/subscriptions/run` + ban/unban helpers
- `apps/api/src/config.ts` — env: `ADMIN_DASHBOARD_TOKEN`, `MAIN_CHAT_ID`, `EARLYRISE_PUBLIC_CHAT_INVITE_URL`

### Известные ограничения / что дальше
- Миграция Supabase через MCP сегодня не применилась (timeout). Код работает “best-effort” и не требует новых колонок, но колонки полезны для прозрачности.
- Если понадобится “жёстко ограничивать доступ” помимо бота (например, в групповых обработчиках) — нужно добавить проверку `expired` и там.

